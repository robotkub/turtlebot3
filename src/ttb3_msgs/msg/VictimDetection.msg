# Published by victim_detector on /victim_detections.
# bearing: normalized pixel-x offset from image center, -1 (left edge) .. +1 (right edge).
# apparent_size: bbox area / image area, 0..1 — used as a distance proxy (bigger = closer).
bool detected
float32 bearing
float32 apparent_size
float32 image_x
float32 image_y
